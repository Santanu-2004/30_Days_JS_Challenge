//Task-11:  WAP that uses the && operator to combine two conditions and log the result to console
if (5<8 && 8>9){
    console.log(`Both The conditions are true`);
}else{
    console.log(`One of the condition or both the conditions may wrong`);
}

//Task-12:  WAP that uses the || operator to combine two conditions and log the result to console
if (5<8 || 8>9){
    console.log(`One or Both The conditions may true`);
}else{
    console.log(`Both conditions are wrong`);
}

//Task-13:  WAP that uses the ! operator to negate a conditions and log the result to console

